const d = document,
  $main = d.querySelector("main"),
  $links = d.querySelector(".links");

let pokeAPI = "https://pokeapi.co/api/v2/pokemon/"

async function loadPokemons(url) {
    try {

    } catch (err) {

    }
}
  d.addEventListener("DOMContentLoad, e=> loadPokemons('pokeAPI'));
